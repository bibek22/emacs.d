(define (mult x y)
  (define (double x)
    (+ x x))
  (define (halve n)
    (if (even? n)
	(/ n 2)
	()))
  (define (mult-itr x y)
    (if (not (odd? y))
	(mult-itr (double x) (halve y))
	(+ (mult-itr (double x) (halve (- y 1))) x))))
